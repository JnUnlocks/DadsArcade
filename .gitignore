node_modules/
dist/

# Wrangler's local state, including the local D1 database
.wrangler/
.dev.vars

# Per-machine Claude Code settings -- personal to whoever cloned this, not
# something the repo should dictate. The shared launch.json stays tracked.
.claude/settings.local.json

.DS_Store
*.log
